[package]
name = "alizarin-extension-api"
version = "2.0.0-alpha.87"
edition = "2021"
description = "C ABI types for Alizarin extensions"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["lib"]
