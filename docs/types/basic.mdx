---
title: Basic types
description: string, number, boolean and url view models
---

import AlizarinComponent from '@/app/alizarin.tsx';

The basic scalar view models wrap primitive-like Arches datatypes. They resolve
to a value you can render directly — with one caveat below.

## string

`StringViewModel` wraps a localized `string`. It **extends `String`**, so it
stringifies to the value in the [current language](/docs/types/i18n) and can be
rendered directly in JSX. `non-localized-string` (`NonLocalizedStringViewModel`)
is the single-language variant.

## number

`NumberViewModel` wraps a numeric value. Like the other non-string scalars it is
an object, not a primitive — coerce it (`` `${value}` `` or `String(value)`)
before rendering, or React will reject a raw `Number`/`Date` object as a child.

## boolean & url

`BooleanViewModel` extends `Boolean` and renders a **label** from the node config
(`trueLabel` / `falseLabel`) rather than `true`/`false`. `UrlViewModel` wraps a
`{ url, url_label }` value — use `.forJson()` to get both, or stringify for the
label. Both are objects, so coerce before rendering.

## Live: string + number

Each site's **name** (`string`) and its **area** (`number`, nested in a semantic
nodegroup) — press ▶:

<AlizarinComponent module='/example/example-9.tsx' />

The `area` is coerced with a template literal, since a raw `NumberViewModel`
object isn't a valid React child.

## Live: boolean + url (+ edtf)

Each site's **protected** flag (`boolean`, shown via its config label), a
**record link** (`url`), and its **survey period** (`edtf`, see
[Dates & EDTF](/docs/types/dates-edtf)) — press ▶:

<AlizarinComponent module='/example/example-12.tsx' />
